# NOTE:
# add absolute path of the files to be maintained here(the `~` and `*` also allowed) 
# each one line. empty line or line starts with `#` will be ignored
#
#
# Example:
#     ~/.vim/
#     ~/foo bar/path_with_space.sh
#     ~/.vimrc
#     /etc/locale.gen
#     /etc/hosts
#     /home/kelvin/.fonts/UbuntuMono-*.ttf


# in ~
~/.vim/
~/.config/font-manager/local.conf
~/.fonts/UbuntuMono-*.ttf
/etc/hosts
/etc

# in /
